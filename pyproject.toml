[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"


[project]
name = "pretty_pdf"
version = "0.0.1"
dependencies = [
    "typer"
]


[project.optional-dependencies]
dev = ['pytest']